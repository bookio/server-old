# Readme

## Setup
If RMV is present Ruby version will follow .ruby-version and use .ruby-gemset

Setup local gemset:
  bundle install

## Tests

Setup test database
  RAILS_ENV=test rake db:setup

Run tests using Rspec
  bundle exec rspec
